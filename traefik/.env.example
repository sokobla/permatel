# Traefik (reverse-proxy partagé du serveur) — copier en .env puis renseigner.
# ⚠ Ce .env est propre à ce stack, distinct de celui de chaque application.

# Adresse notifiée par Let's Encrypt (expiration proche, révocations…).
ACME_EMAIL=admin@exemple.com
